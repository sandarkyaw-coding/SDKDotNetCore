suggestion => crtl + . 

break point => F9
for each line => F10
for details => F11

connection -> default value = null 

dataset => dataTable
dataTable => dataRow
dataRow => dataColumn

parameter = @ sign => insert value